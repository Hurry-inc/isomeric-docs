---
title: "Data Extraction"
api: "POST https://api-preview.isomeric.ai/extract"
description: "Use this endpoint to design your own prompt for transforming website text into structured JSON."
---

### Headers

<ParamField
  header="Authorization"
  required="true"
  initialValue="Bearer iso_abc123"
>
  Use your API key to authenticate the request.
</ParamField>

### Params

<ParamField body="Query Parameters" type="object" children="Optional additional query parameters.">
  <Expandable title="Toggle object">
    <ParamField body="include_content" type="string">
      Set to `1` to return the raw content from the target website
    </ParamField>

  </Expandable>
</ParamField>

### Body

<ParamField body="JSON request" type="object" required="true" children="A JSON object representing the request you are making to the Isomeric extraction engine.">
  <Expandable title="Toggle object">
    <ParamField body="url" type="string" required="true">
      The URL of the page you'd like to extract data from.
    </ParamField>

    <ParamField body="prompt" type="object" required="true">
      The JSON schema representing the data you would like extracted.

      See our [guide on prompt design](./api-reference/prompting) for more info.
    </ParamField>

  </Expandable>
</ParamField>

### Response

<ResponseField name="credits" type="object">
  Info about API credit usage for this request.

  <Expandable title="Toggle object">
    <ResponseField name="remaining" type="number">
      The number of credits remaining after this call
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data" type="object">

The data returned from the extraction request.

{" "}

<Expandable title="Toggle object">
  <ResponseField name="keys..." type="object">
    The `data` field will contain a JSON object with keys and values mapped to
    your original schema.
  </ResponseField>
</Expandable>

</ResponseField>

<RequestExample>

```bash Example Request
curl -H "Authorization: Bearer <token>" -d '{
    "url": "https://auraframes.com/digital-frames/color/carver-mat",
    "prompt": {
        "type": "object",
        "properties": {
            "title": { "type": "string" },
            "brand": { "type": "string" },
            "price": {
                "type": "string",
                "pattern": "\\d+\\.\\d{2}"
            },
            "currency_code": {
                "type": "string"
            },
            "color": {
                "type": "string",
            }
        }
    }
}' https://api-preview.isomeric.ai/extract
```

</RequestExample>

<ResponseExample>

```json Response
{
  "credits": {
    "remaining": 4832
  },
  "data": {
    "brand": "Aura",
    "color": "Gravel with White Mat",
    "currency_code": "USD",
    "price": "179.00",
    "title": "Carver Mat"
  }
}
```

</ResponseExample>
