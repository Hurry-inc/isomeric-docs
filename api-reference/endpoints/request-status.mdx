---
title: "Get Request Status"
api: "GET https://api-preview.isomeric.ai/request-status/:id"
description: "This endpoint returns the status of an `/extract` request. You may poll for job status at a reasonable interval of no less than 500ms."
---

### Headers

<ParamField
  header="Authorization"
  required="true"
  initialValue="Bearer iso_abc123"
>
  Use your API key to authenticate the request.
</ParamField>

### URL Path Params

<ParamField body=":id" type="string" required="true">
  The request (job) ID returned from your call to `/extract`
</ParamField>

### Response

<ResponseField name="status" type="object">
  The status of the requested request (job). Values will be one of `completed`,
  `inflight`, `failed`. In flight requests are still being processed.
</ResponseField>

<ResponseField name="requestUUID" type="string">
    The UUID of the request (job) which you can use to poll status.

    Note: Only present when the `mode` is not `sync`.

</ResponseField>

<ResponseField name="data" type="object">

The data returned from the extraction request, which is identical to that from `/extract`. Only present when the request status is `completed`.

Note: Only present when the `mode` is `sync`.

{" "}

<Expandable title="Toggle object">
  <ResponseField name="keys..." type="object">
    The `data` field will contain a JSON object with keys and values mapped to
    your original schema.
  </ResponseField>
</Expandable>

</ResponseField>

<RequestExample>

```bash Example Request
curl -s 'https://api-preview.isomeric.ai/request-status/36099fab-4771-4ea6-bd97-fa6152b9970d' \
-H 'Authorization: Bearer <token>' \
```

</RequestExample>

<ResponseExample>

```json Response
{
  "status": "inflight"
}
```

</ResponseExample>
